
  # Wellpack

  This is a code bundle for Wellpack. The original project is available at https://www.figma.com/design/n75lP2g69FAevBzs8LNM3i/Wellpack.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  